source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll', '3.9.5'
  gem 'jekyll-feed', '~> 0.17'
  gem 'jekyll-sitemap', '~> 1.4'
  gem 'jekyll-redirect-from', '~> 0.16'
  gem 'webrick', '~> 1.8'
end

# Local development is using macOS system Ruby 2.6.10. The current
# github-pages meta-gem resolves Ruby-3-only native gem releases, so this
# Gemfile pins direct dependencies that still support Ruby 2.6.
gem 'ffi', '1.15.5'
gem 'connection_pool', '2.5.0'
